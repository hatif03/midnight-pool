{
  "name": "midnight-pool-contracts",
  "version": "0.1.0",
  "description": "Compact smart contract for Midnight Pool: stat commitments, threshold credentials, soulbound cue claims, and a provably-fair break-order coin flip.",
  "license": "MIT",
  "private": true,
  "type": "module",
  "scripts": {
    "compile": "compact compile midnight-pool.compact managed/midnight-pool && compact compile stakes.compact managed/stakes",
    "compile:fast": "compact compile --skip-zk midnight-pool.compact managed/midnight-pool && compact compile --skip-zk stakes.compact managed/stakes",
    "format": "compact format midnight-pool.compact && compact format stakes.compact",
    "typecheck": "tsc --noEmit",
    "test": "tsx test/simulator.test.ts && tsx test/stakes.test.ts"
  },
  "dependencies": {
    "@midnight-ntwrk/compact-runtime": "0.19.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.21.0",
    "typescript": "^5.9.0"
  }
}
